{
    "name": "supertest_tests",
    "version": "0.1.0",
    "scripts": {
        "test": "cd test && mocha *.js",
        "mock": "json-server mockEndpoint/customers.json"
    },
    "devDependencies": {
        "chai": "^4.1.2",
        "faker": "^4.1.0",
        "json-server": "^0.12.1",
        "mocha": "^5.1.0",
        "supertest": "^3.0.0"
    }
}
